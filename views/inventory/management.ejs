<% if (title) { %>
    <h1 class="inventory-management-title">
        <%= title %>
    </h1>
    <% } else { res.redirect('/') } %>

        <p><%- flashMessage %></p>

        <div class="management-links">
            <a href="/inv/add-classification" class="btn">Add New Classification</a>
            <a href="/inv/add-inventory" class="btn">Add New Inventory</a>
        </div>